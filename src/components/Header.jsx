import {useEffect,useState}from'react';
const links=[['About','#about'],['What we do','#what-we-do'],['Projects','#projects'],['Governance','#governance']];
export default function Header(){const[scrolled,setScrolled]=useState(false);const[open,setOpen]=useState(false);useEffect(()=>{const fn=()=>setScrolled(window.scrollY>50);window.addEventListener('scroll',fn,{passive:true});return()=>window.removeEventListener('scroll',fn)},[]);return <header className="fixed inset-x-0 top-0 z-50 px-4 pt-4 sm:px-7"><div className={`mx-auto flex max-w-6xl items-center justify-between rounded-full px-5 py-2.5 transition-colors ${scrolled?'bg-paper shadow-md':'bg-ink/35 text-paper backdrop-blur-md'}`}><a href="#top" className="flex items-center gap-2.5"><span className={`grid h-8 w-8 place-items-center rounded-lg ${scrolled?'bg-ink text-paper':'bg-paper/15'}`}>C</span><span><b className="block font-display text-sm">CSPARK</b><small className="block text-[8px] uppercase tracking-[.13em] opacity-70">Spatial planning · Kenya</small></span></a><nav className="hidden gap-5 lg:flex">{links.map(([label,href])=><a key={href} href={href} className="text-sm font-medium hover:text-plot">{label}</a>)}</nav><div className="flex items-center gap-3"><a href="#contact" className="hidden rounded-full bg-plot px-4 py-2 text-sm font-semibold text-white sm:block">Partner with us</a><button onClick={()=>setOpen(!open)} className="grid h-8 w-8 place-items-center rounded-full border border-current lg:hidden" aria-label="Toggle menu" aria-expanded={open}>{open?'×':'+'}</button></div></div>{open&&<nav className="mx-auto mt-2 max-w-6xl rounded-3xl bg-paper p-6 shadow-lg lg:hidden">{links.map(([label,href])=><a key={href} onClick={()=>setOpen(false)} href={href} className="block border-b border-line py-4 font-display text-2xl last:border-0">{label}</a>)}<a href="#contact" onClick={()=>setOpen(false)} className="button-plot mt-5">Partner with us</a></nav>}</header>}
