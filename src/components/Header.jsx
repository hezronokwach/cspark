import{useEffect,useState}from'react';
const links=[['Home','#top'],['About us','#about'],['What we do','#what-we-do'],['Projects','#projects'],['Governance','#governance']];
export default function Header(){const[open,setOpen]=useState(false);const[active,setActive]=useState('#top');useEffect(()=>{const fn=()=>{const found=[...document.querySelectorAll('main section[id]')].findLast(s=>s.getBoundingClientRect().top<160);if(found)setActive(`#${found.id}`)};window.addEventListener('scroll',fn,{passive:true});fn();return()=>window.removeEventListener('scroll',fn)},[]);return <header className="fixed inset-x-0 top-0 z-50 bg-white shadow-sm"><div className="frame flex h-20 items-center justify-between"><a href="#top" className="font-display leading-none"><span className="block text-2xl font-extrabold tracking-[-.07em]">CSPARK</span><span className="mt-1 block text-[8px] font-semibold uppercase tracking-[.12em] text-plot">Planning advocacy · Kenya</span></a><nav className="hidden h-full items-center gap-8 lg:flex">{links.map(([label,href])=><a key={href} href={href} className={`flex h-full items-center border-b-[3px] font-display text-sm font-semibold transition-colors ${active===href?'border-plot text-ink':'border-transparent text-ink hover:border-plot/50'}`}>{label}</a>)}</nav><a href="#contact" className="hidden bg-plot px-7 py-4 font-display text-sm font-bold text-white md:block">Partner with us</a><button className="md:hidden font-display text-2xl" onClick={()=>setOpen(!open)} aria-label="Toggle menu">{open?'×':'☰'}</button></div>{open&&<nav className="border-t border-line bg-white px-5 pb-5 md:hidden">{links.map(([label,href])=><a onClick={()=>setOpen(false)} key={href} href={href} className="block border-b border-line py-4 font-display text-lg font-bold">{label}</a>)}<a onClick={()=>setOpen(false)} className="mt-5 block bg-plot px-5 py-4 text-center font-display font-bold text-white" href="#contact">Partner with us</a></nav>}</header>}
