import{useEffect,useRef}from'react';import gsap from'gsap';
const projects=[['Lake Victoria Basin','Marine spatial planning','We brought together communities and county governments to plan for fishing, transport, energy and conservation.'],['Kisumu','Support for street traders','We worked with traders to collect the evidence needed for better markets and safer places to work.'],['Nairobi and Nakuru','Settlement mapping','We mapped homes and services so families could take part in decisions about settlement upgrades.']];
export default function Projects(){const ref=useRef(null);useEffect(()=>{if(window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;const c=gsap.context(()=>gsap.fromTo('.project-card',{opacity:0,y:70},{opacity:1,y:0,duration:.75,stagger:.14,scrollTrigger:{trigger:ref.current,start:'top 76%',once:true}}),ref);return()=>c.revert()},[]);return <section id="projects" ref={ref} className="bg-paper py-28"><div className="frame"><div className="mx-auto max-w-3xl text-center"><p className="mono-label">Selected projects</p><h2 className="display mt-4 text-5xl font-extrabold leading-tight">Change begins with a clear picture of what is happening.</h2><p className="mt-5 text-lg leading-relaxed">Our projects help people turn local knowledge into better choices.</p></div><div className="mt-16 grid gap-6 lg:grid-cols-3">{projects.map(([place,title,copy])=><article className="project-card flex min-h-[520px] flex-col bg-white opacity-0" key={title}><div className="image-slot h-56" role="img" aria-label={`Image placeholder for the ${title} project`}/><div className="flex flex-1 flex-col p-7"><p className="font-display text-sm font-bold text-plot">{place}</p><h3 className="display mt-4 text-3xl font-extrabold leading-tight">{title}</h3><p className="mt-5 leading-relaxed text-map">{copy}</p><a href="#contact" className="text-link mt-auto pt-8">View project <span>→</span></a></div></article>)}</div></div></section>}
