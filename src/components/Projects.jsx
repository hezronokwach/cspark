import {useEffect,useRef}from'react';import gsap from'gsap';import{ScrollTrigger}from'gsap/ScrollTrigger';gsap.registerPlugin(ScrollTrigger);
const projects=[['Lake Victoria Basin','Marine spatial planning','We helped bring communities and county governments together to plan for fishing, transport, energy and conservation.','Lake Victoria'],['Kisumu','Support for street traders','We worked with traders to collect the evidence needed for better markets and safer places to work.','Kisumu Market'],['Nairobi and Nakuru','Settlement mapping','We mapped homes and services so families could take part in decisions about settlement upgrades.','Neighbourhood map']];
export default function Projects(){const ref=useRef(null);useEffect(()=>{if(window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;const ctx=gsap.context(()=>{gsap.to('.project-strip',{xPercent:-24,ease:'none',scrollTrigger:{trigger:ref.current,start:'top bottom',end:'bottom top',scrub:1}})},ref);return()=>ctx.revert()},[]);return <section id="projects" ref={ref} className="overflow-hidden bg-paper py-28"><div className="frame"><div className="mx-auto max-w-3xl text-center"><p className="text-sm font-semibold text-plot">Selected projects</p><h2 className="display mt-4 text-4xl leading-tight sm:text-6xl">Work that helps people take part in decisions.</h2><p className="mt-5 text-lg leading-relaxed text-map">Our projects start with the people who know a place best.</p></div></div><div className="project-strip mt-16 flex w-[145%] gap-5 pl-5 sm:w-[120%] sm:pl-8 lg:w-[105%] lg:pl-12">{projects.map(([place,title,copy,label],i)=><article key={title} className="flex min-h-[430px] flex-1 basis-0 flex-col rounded-3xl bg-white p-7 sm:p-9"><div className={`grid h-40 place-items-center rounded-2xl ${i===0?'bg-ochre':i===1?'bg-plot':'bg-map'} text-paper`}><span className="display max-w-[12rem] text-center text-3xl leading-tight">{label}</span></div><p className="mt-7 text-sm font-semibold text-plot">{place}</p><h3 className="display mt-3 text-3xl leading-tight">{title}</h3><p className="mt-4 text-sm leading-relaxed text-map">{copy}</p><a href="#contact" className="mt-auto pt-7 text-sm font-semibold text-plot">Read the project →</a></article>)}</div></section>}
